import Container from "../../../components/UI/Container";
import classes from "./style.module.scss";
const Timeline = () => {
    return (
        <Container className={classes.container}>This is Timeline</Container>
    );
};

export default Timeline;
